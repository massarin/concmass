[build-system]
requires = ["setuptools>=77.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "concmass"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "scipy", "colossus", "matplotlib", "pytest"]

[tool.setuptools.packages.find]
exclude = ["figures*"]
